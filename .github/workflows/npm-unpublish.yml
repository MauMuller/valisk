name: Unpublish package to NPM

on: workflow_dispatch

jobs:
  unpublish: # JOB
    runs-on: ubuntu-latest
    steps:
      - name: Unpublishing packeage
        run: npm unpublish --force ${{ github.repository }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.PUBLISH_NPM }}
